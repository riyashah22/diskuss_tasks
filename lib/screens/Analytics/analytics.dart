import 'package:flutter/material.dart';

class AnalyticsScrren extends StatelessWidget {
  const AnalyticsScrren({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Analytics"),
    );
  }
}
